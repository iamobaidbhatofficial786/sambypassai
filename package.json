{
  "name": "lovpropro-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd admin-dashboard && npm install && npm run build && cp -r .next ../ && cp -r public ../"
  },
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
